# ServiceAccount used by the console Deployment. Bound to the ClusterRole in
# rbac.yaml which grants scoped read access to cluster resources plus full
# access to openinfra.dev Application objects (the product CRD).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: console
  namespace: open-infra-console
